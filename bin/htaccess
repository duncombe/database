# template htaccess file. copy it to .htaccess in the folder you wish to
# protect and change valid-user to "user <username>" in the htpasswd file if
# you do not want any valid user to access the folder. 

AuthType Basic
AuthName "Restricted Content"
AuthUserFile /etc/httpd/htpasswd
Require valid-user
